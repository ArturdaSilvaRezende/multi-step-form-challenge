import styled from "styled-components";

const GlobalPrevButton = styled.button`
  color: var(--Cool-gray);
  font-weight: 700;
  transition: 0.3s ease;

  &:hover {
    color: var(--Marine-blue);
  }
`;

export default GlobalPrevButton;
